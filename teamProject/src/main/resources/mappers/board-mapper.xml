<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.team.mappers.board">


	<!-- 글 등록  -->
	<insert id="create">
		insert into tal_board(board_no, board_title, board_content, writer, re_group)
		values(#{board_no}, #{board_title}, 
			#{board_content, jdbcType=VARCHAR}, #{writer},
			#{board_no})
	</insert>
	
	
	
	
</mapper>